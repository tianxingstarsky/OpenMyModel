{
  "name": "openmymodel-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Local WebSocket tunnel and regression tests",
  "main": "cloud_bridge.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:release": "node sync_bridge.js --check",
    "sync:release": "node sync_bridge.js"
  },
  "dependencies": {
    "ws": "^8.21.0"
  }
}
