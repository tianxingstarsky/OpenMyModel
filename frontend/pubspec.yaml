name: openmymodel
description: "OpenMyModel - 本地算力云端共享"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: ^4.10.0
  http: ^1.3.0
  web_socket_channel: ^3.0.2
  file_picker: ^8.1.0
  path_provider: ^2.1.5
  shared_preferences: ^2.3.5
  provider: ^6.1.2
  file_selector: ^1.0.3
  file_selector_windows: ^0.9.3
  window_manager: ^0.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true